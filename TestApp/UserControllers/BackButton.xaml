<UserControl x:Class="TestApp.UserControllers.BackButton"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TestApp.UserControllers"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Style="{StaticResource BackButtonFontStyle}">
    <Grid >
        <Button Content="{DynamicResource Back}" Command="NavigationCommands.BrowseBack" 
                Background="{DynamicResource Background}"
                HorizontalAlignment="Left" VerticalAlignment="Top" 
                Foreground="{DynamicResource Text}"
                Margin="10 10 0 0"
                BorderThickness="0"/>
    </Grid>
</UserControl>
